//*    -- --- --- map()---- ----  *//
//* if you need new array from old array with some changes *//

let numbers = [1,2,3,4,5,6,7]

// let doubledArray = numbers.map((number) =>{
//     return number * 2;
// })
//*    same way to do that*//

let doubledArray = numbers.map((number) => number * 2
)

console.log(doubledArray)//[2, 4, 6, 8, 10, 12, 14]